import styled from 'styled-components'

export const Detalhes = styled.div`
  display: flex;
  flex-direction: column;

  .container {
    display: flex;
    flex-wrap: wrap;
    gap: 32px;
    padding: 40px 0;
  }
`
